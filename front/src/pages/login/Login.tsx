import { MainForm } from '../../common/form'
import { LoginHeader } from './styled'

export const Login = () => {
    return(
        <MainForm>
            <LoginHeader />
        </MainForm>
    )
}